using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SubjectiveTest : MonoBehaviour
{

	/*
     * Subjective Test has an array of tasks
     * The array can be updated by the user as they desire
     */

	//[Rename("Tasks", "Task")]
	//[SerializeField]
	public List<TestTask> tasks;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
