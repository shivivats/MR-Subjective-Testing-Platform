using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SubmitUserFeedback : MonoBehaviour
{
    // a starting scene will ask the user for their name

    // then this object in the main scene will record the user's ratings
    // and store them in a JSON file or something when the user presses the "submit" button


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
