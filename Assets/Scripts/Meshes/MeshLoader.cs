using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MeshLoader : MonoBehaviour
{

    // This script is in charge of loading the meshes

    // Load the meshes or at least store paths to the meshes and animations

    // See how to animate the meshes


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
