using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConfigurationManager : MonoBehaviour
{

    // Have a UI control panel like thing for this - Hand Menu or Near Menu
    // look at Hand Menu examples for sliders and buttons

    // add or remove gameobjects to display multiple objects at once

    // for each gameobject, be able to choose PC, mesh or shader, quality, distance, add stalling

    // also be able to switch between PC and mesh for each gameobject (get mesh from ghent)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
